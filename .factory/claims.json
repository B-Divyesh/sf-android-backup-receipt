[
  {
    "id": "demo-sample-receipt",
    "claim": "Try a four-file sample backup check in one click.",
    "where": "Landing primary action, demo banner",
    "test": "npm run test:claims -- --grep @claim:demo-sample-receipt",
    "sandbox": "Fresh browser context at /demo; assert the isolated demo receipt has 2 accounted, 1 missing, 1 changed, and 50% coverage."
  },
  {
    "id": "local-only-files",
    "claim": "Selected files stay on your device.",
    "where": "Landing facts, workspace guidance, privacy policy",
    "test": "npm run test:claims -- --grep @claim:local-only-files",
    "sandbox": "Fresh browser context at /demo; record all requests during the sample flow and assert every request is same-origin."
  },
  {
    "id": "receipt-exports",
    "claim": "Export a JSON receipt and CSV details.",
    "where": "Landing facts, receipt actions, README",
    "test": "npm run test:claims -- --grep @claim:receipt-exports",
    "sandbox": "Fresh browser context at /demo; download both exports and assert schema, header, and discrepancy rows."
  },
  {
    "id": "sha256-evidence",
    "claim": "The sample receipt includes SHA-256 file evidence.",
    "where": "How it works, README",
    "test": "npm run test:claims -- --grep @claim:sha256-evidence",
    "sandbox": "Fresh browser context at /demo; export the sample source manifest and assert four SHA-256 evidence records."
  },
  {
    "id": "offline-reload",
    "claim": "The check works offline after the first visit.",
    "where": "README",
    "test": "npm run test:claims -- --grep @claim:offline-reload",
    "sandbox": "Fresh browser context at /?demo=1; wait for the service worker, set offline, reload, and assert the sample receipt remains visible."
  }
]
